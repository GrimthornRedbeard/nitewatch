# nitewatch — Orchestrator Status

> Per-project orchestrator state, split out of the root `ORCHESTRATOR.md` on 2026-08-05.
> The root doc is now a router; **this file is the source of truth** for this project's
> status and rotation history. Update it in place at the end of every rotation, then
> refresh the one-line status in the root router.

## Current

- **Rotation slot:** #15
- **Git remote:** GrimthornRedbeard/nitewatch
- **Active plan / status:** **PUBLIC PRE-RELEASE — `v0.1.2-pre` live at https://threattape.com/products/nitewatch/#build** (SHA-256 `9504f43f…`, verified against the served file). Consumer Windows EDR-lite; P1–P4 built. Sparks cleared the disclaimer; **code signing dropped (Kevin: not paying)** — Smart App Control will refuse to run it on Win11, stated plainly on the download page. **Two attribution bugs fixed 2026-07-28/29 — recycled PIDs and recycled ETW file handles** — the second found by an overnight soak that blamed four unrelated programs for reading Discord's session tokens. **Open unknown: the quiet-machine false-positive rate.** Soak restarted 2026-07-29 on 0.1.2-pre; every earlier run measured a build that manufactured its own false positives, so there is no baseline yet. Next rotation: read the soak, then Gates 4–5 (QA sweep + smoke test), never formally run on this project.

<!-- audit:start -->
- **Audited 2026-08-10:** 🔵 Public pre-release v0.1.6-pre is live and hash-verified end to end, but every status doc still says v0.1.2-pre. Zero issues/PRs is silence, not health: gates 3-5 unrun on the shipped build.
- **Where it stands:** NiteWatch is a feature-complete consumer Windows EDR-lite (P1-P4 all built: flight recorder, causal graph, 14 detection rules across 4 packs, one-click response with undo) shipping as a single 14 MB static Go exe with an embedded dashboard. It is genuinely published: I downloaded https://threattape.com/products/nitewatch/download/nitewatch.exe and its SHA-256 (be9c2bf2a2ad49241910d059ceddde2d472cf6cd1c9b9eb65de3aebd051f8a5a) matches the hash printed on the page, the local /home/grimmy/Projects/nitewatch/dist/nitewatch.exe, and the copy in the threattape repo — and `go version -m` stamps it vcs.revision=d1020c3, modified=false, which is tag v0.1.6-pre from a clean tree. The last real engineering was 2026-08-02 through 08-04: a 3-day soak on Kevin's desktop produced five alerts, all false positives, which were fixed as three named classes (Defender scanning credential stores from ProgramData rather than System32, Chromium browsers importing each other's password stores, browsers beaconing) with no threshold changes; then a dashboard fix where the stat tiles counted the fetched page (1,000) instead of the ledger (22,087), then a favicon. Nothing has been committed since 2026-08-05, which was just the ORCHESTRATOR.md split. What does not exist: any recorded test run, QA sweep, or smoke test against the code the public is currently downloading — the last QA sweep is dated 2026-07-27, twenty-two commits and four tagged releases ago, and the Windows-VM smoke test that both the P1 and P2 plans list as owed has never been run at all.
- **Next action:** Run tests + a fresh QA sweep against tag v0.1.6-pre, then correct the v0.1.2-pre claim in both status docs.
<!-- audit:end -->

## Roadmap

_Updated 2026-08-10 by portfolio audit._

**Done means:** For a released desktop tool with no bug reports, 'done' cannot mean 'no open issues' — nobody is filing any, and the product's privacy design forbids the telemetry that would tell you whether it is even being used. v1 means four concrete things. (1) A published false-positive number from a soak on current code: a week of quiet running that produces a count Kevin can put on the download page in place of 'unmeasured', which the shipped roadmap itself calls the gate that makes everything else irrelevant if failed. (2) The Windows-VM smoke test that P1 and P2 both marked owed — live ETW capture, elevation, a Run-key persistence trigger, a feed-flagged connection, the toast, and at least one response action with its undo, driven on real Windows rather than replay on WSL2. (3) An installer plus Windows service with the SQLite ledger relocated to %ProgramData% with an ACL, because a console app started by hand is not watching when nobody is looking, and that is also the reason a week-long soak is awkward to run. (4) A repeat of gates 3 and 4 against the exact tag that is published, with output pasted, so the artifact strangers download has actually been through the contract. Until then it is an honest pre-release, not a v1.

1. **Run the full gate set against the published tag v0.1.6-pre** — Check out v0.1.6-pre, run `go test -race ./...`, `go vet` for both linux and windows targets, `gofmt -l`, `govulncheck`, and the CGO-free cross-compile, paste the output, and write a new docs/reports/qa-sweep-2026-08-xx.md. Then correct nitewatch/ORCHESTRATOR.md and row 15 of the root /home/grimmy/Projects/ORCHESTRATOR.md, both of which still describe v0.1.2-pre and the superseded SHA 9504f43f as what is live. _(effort: hours)_
   - _Why now:_ A binary is sitting on a public website right now whose current source has never been through a recorded verification pass. The last evidence is the 2026-07-27 sweep (17/17 packages green) and a 2026-07-28 note of 23 packages green under -race; since then the ETW filename cache was rewritten, the project was relicensed to GPL-3.0, three detector suppressions were added, and a new /api/summary endpoint shipped. This is hours of work and it is the cheapest thing on the list.
2. **Land a false-positive number and replace 'unmeasured' on the download page** — Run a multi-day quiet-machine soak on v0.1.6-pre — the first build where all three false-positive classes from the 2026-08-02 retune are fixed — collect it with agent/scripts/soak-report.py, and publish the alert count and duration on the download page and in the shipped known-limitations document. _(effort: weeks)_ · Blocked by: Needs Kevin's real Windows desktop running the agent for several consecutive days; it cannot be produced on the WSL2 dev box, and without a Windows service the agent only runs while a console window is open.
   - _Why now:_ The shipped roadmap says in its own words that nothing else on the list matters if the product cries wolf, and the download page currently tells visitors 'nobody has measured how often it cries wolf'. A 3-day measurement does exist (five alerts, all wrong) but it measured pre-retune code, so the honest number is still missing. This is the single claim on a public page that is both the most important to the product's credibility and the most likely to go stale silently.
3. **Do the owed Windows-VM smoke test** — Stand up a disposable Windows VM, run the elevated agent live, trigger a Run-key persistence entry and a connection to a feed-flagged address, and confirm the toast fires, the alert renders with the correct causal story, and at least one response action executes and undoes cleanly. _(effort: days)_ · Blocked by: Needs a Windows VM or test host; the dev environment is WSL2 and the project has never had one provisioned.
   - _Why now:_ Both the P1 and P2 plan documents list this as explicitly owed, and it is the only gate that can exercise the code paths a WSL2 dev box structurally cannot reach: real ETW capture, elevation, Windows toasts, firewall rule creation, and service install. Every attribution bug this project has shipped so far lived in exactly that Windows-only region.
4. **Installer, Windows service, and the database in %ProgramData%** — Package the exe into an installer that registers it as a Windows service and relocates the SQLite ledger from beside the executable into %ProgramData% with an appropriate ACL. _(effort: weeks)_
   - _Why now:_ It is the top item in the shipped roadmap's 'Next' section and the download page admits it as a limitation twice. It is also a prerequisite for the soak above being practical rather than a chore Kevin has to babysit, and the database relocation is explicitly gated on the installer existing.
5. **Build the replayable false-positive regression suite** — Record a week of ordinary desktop activity as a replay trace and wire it into the test suite with the alert count as a build gate, so a tuning change that reintroduces a suppressed false-positive class fails locally rather than on a user's machine. _(effort: days)_ · Blocked by: Depends on the soak producing a recorded trace of real desktop activity to replay.
   - _Why now:_ The three suppressions added on 2026-08-02 each have unit tests, but there is no corpus-level check that the whole detector set stays quiet on a real machine's traffic. Once the soak above produces a trace, capturing it as a fixture is nearly free, and it converts a manual multi-day measurement into something that runs on every commit.

### Risks

- The unsigned-binary consequence is disclosed thoroughly and accurately — the download page states before the button ('Pre-release. Unsigned. Windows-only. Read the next four paragraphs before you run it'), after the button under 'What will happen when you run it' ('On Windows 11 with Smart App Control switched on, it won't run at all; that feature blocks unsigned executables outright'), in the hero status badge, and in the limitations list higher up the page, and it is also compiled into the binary in agent/internal/help/known-limitations.md and roadmap.md — so this is not a trust problem, but it does mean an unknown fraction of Win11 visitors download a file they physically cannot execute and have no way to report that back.
- The false-positive rate on the code that is actually published is unmeasured, and this is the one number the product's own roadmap says invalidates everything else if it is bad.
- There is no feedback channel that produces data: the privacy design forbids telemetry, the only reporting route is an email address, and in the ~13 days since the download went public zero GitHub issues have been filed against the three issue templates that were built for exactly this — so 'zero open issues' is indistinguishable from 'nobody has run it'.
- Every status document in the portfolio describes a build four tagged releases behind what is actually being served, which means any decision Kevin makes from the router or ORCHESTRATOR.md is being made against a build from 2026-07-29.
- This project has already shipped two separate attribution bugs that named innocent real programs as credential thieves (recycled PIDs, then recycled ETW file handles falsely accusing four programs of reading Discord session tokens) — a third class of the same shape would now land on strangers' machines rather than Kevin's, against a product whose entire pitch is that it tells you the truth about what your computer did.
- The GPL-3.0 licence is inherited from the golang-etw dependency rather than chosen, so any future commercial packaging of NiteWatch must ship source, and that constraint is load-bearing on the business model of the portfolio's only consumer product.
- Threat-feed licensing is unresolved: several high-quality sources cannot be used commercially without written permission, and caching feed data on a user's machine counts as redistribution rather than use.
- The download page claims 'a week-long quiet-machine soak is running right now', which I could not verify and which most likely describes the soak that concluded before the 2026-08-02 retune — on a page whose credibility is built entirely on being scrupulously exact about what is and is not known, a stale present-tense claim is a disproportionate liability.
- Six git tags exist for six pre-releases but no GitHub Releases have been published, so a reader following the page's 'tagged per release' pointer to the Releases tab finds an empty list and no independent copy of the artifact or its hash.

### Known doc drift

_Places where this repo's documentation contradicts reality. Fixing these is cheap and keeps future sessions from acting on stale premises._

- nitewatch/ORCHESTRATOR.md 'Current' says the live build is v0.1.2-pre with SHA-256 9504f43f. The served file is v0.1.6-pre with SHA-256 be9c2bf2a2ad49241910d059ceddde2d472cf6cd1c9b9eb65de3aebd051f8a5a — verified by downloading it — four tagged releases and eight commits newer.
- Row 15 of the root /home/grimmy/Projects/ORCHESTRATOR.md router repeats the same stale 'Public pre-release v0.1.2-pre' claim.
- nitewatch/ORCHESTRATOR.md's rotation history ends at 2026-07-29. Nothing records the GPL-3.0 relicense and copyright assertion (v0.1.3-pre, 56711ed), the soak retune fixing three false-positive classes (v0.1.4-pre, d6179a6), the stat-tile/ledger-count fix that added /api/summary (v0.1.5-pre, 0bf7920), or the favicon fix (v0.1.6-pre, d1020c3) — four releases with no written history at all.
- nitewatch/ORCHESTRATOR.md states the quiet-machine false-positive rate has 'no baseline yet' and that a soak restarted 2026-07-29. A three-day soak on a real desktop did complete: five alerts, all false positives, resolved into three named classes and fixed on 2026-08-02. The finding exists; only the doc says otherwise.
- The download page says 'A week-long quiet-machine soak is running right now and the number doesn't exist yet.' The page was last edited 2026-08-04 and the soak that produced results concluded before 2026-08-02; whether a fresh post-retune soak is running is unverifiable from the repo. Present-tense claims about ongoing work are the kind of statement that goes stale invisibly.
- nitewatch/ORCHESTRATOR.md has no '## Open Decisions' section, which the portfolio-status tooling parses and which the root CLAUDE.md session-wrap contract requires.
- A tracked zero-byte file named `=` sits in the repository root (a shell-redirect artifact from 2026-07-26) and survived the repo-hygiene cleanup that closed issue #2 ('Repo hygiene: tracked build binary + runtime log, duplicate .bat, gofmt drift').
- Six git tags exist (v0.1.0-pre through v0.1.6-pre) but `gh release list` returns nothing — no GitHub Releases have been published, so the download page's 'Source: github.com/GrimthornRedbeard/nitewatch, tagged per release' leads a reader to an empty Releases tab with no independent artifact or hash.
- docs/plans/ contains four files, none of which is an active plan: the design doc (validated 2026-07-24), P1 and P2 (both delivered), and the cross-platform assessment (a closed decision). P3 and P4 shipped with no plan file, and the real forward-looking plan now lives in agent/internal/help/roadmap.md, which is compiled into the binary and is accurate and current. No REQUIRES_APPROVAL gate exists anywhere in docs/.

## Gate status

_Assessed 2026-08-10. A gate is only green with evidence; an unknown gate is never a pass._

| Gate | State | Evidence |
|---|---|---|
| 1 — zero open issues | ✅ pass | 0 open issues as of 2026-08-10. Only three issues have ever existed (#1 Go toolchain stdlib vulns, #2 repo hygiene, #3 CI workflow policy violation), all closed by 2026-07-27, all filed by the development process rather than by a user. The pass is real but uninformative — no outside reports have arrived since the public download went live on 2026-07-28. |
| 2 — zero open PRs | ✅ pass | 0 open PRs. Three PRs ever (#4, #5, #6), all merged, the last on 2026-07-29. The remote has only the `main` branch; the fix/beacon-fp-and-db-lock, fix/repo-hygiene and fix/toolchain-patch-and-ci-removal refs still visible locally are stale remote-tracking entries for branches already deleted upstream. Local main is in sync with origin/main and the working tree is clean. |
| 3 — automated tests pass locally | ❔ unknown | 50 Go test files across roughly 23 internal packages, plus JSONL replay fixtures in agent/testdata/traces/ and build-tagged manual tests (live_manual_test.go, mem_manual_test.go). No CI exists by portfolio policy — .github/ contains only ISSUE_TEMPLATE, and the offending workflow was deleted under issue #3. Last recorded run: 2026-07-27 QA sweep, `go test -race ./...` 17/17 packages and 184 test funcs green, plus a 2026-07-28 note of 23 packages green under -race. Twenty-two commits have landed since, including the ETW name-cache rewrite (5 new tests) and three new detector suppressions, and nothing records a run against tag v0.1.6-pre — the build the public is downloading. |
| 4 — QA sweep clean | ❌ fail | Two sweeps exist: docs/reports/qa-sweep-2026-07-25.md and docs/reports/qa-sweep-2026-07-27.md. The 07-27 sweep was thorough and clean (go vet, gofmt, govulncheck 0 findings on go1.25.12, gitleaks over 57 commits, loopback binding, API token, DNS-rebinding guard, replay smoke) and found one real bug it fixed. But it predates four tagged releases, the GPL-3.0 relicense, the namecache rewrite, the soak retune, and the /api/summary work. nitewatch/ORCHESTRATOR.md itself states gates 4-5 were 'never formally run on this project' as part of a gate pass. No sweep exists for the published build. |
| 5 — smoke test on a local deploy | ❌ fail | Never run in the form the project's own plans require. Both docs/plans/2026-07-25-p1-flight-recorder.md and docs/plans/2026-07-25-p2-detections.md list a 'Manual Windows-VM smoke test' as owed and it has never happened — no Windows VM has been provisioned. Replay-mode smoke on WSL2 passed in the 2026-07-27 sweep (dashboard plus six API endpoints 200, ransomware.jsonl fires the critical rule with narrative, 6-step playbook and evidence). Live ETW capture, elevation, Windows toasts, firewall rule creation and service install have never been exercised in any recorded run. |

**Active plan:** none on record — _missing_

## Open Decisions (awaiting Kevin)

_Each entry stands on its own: you should be able to answer it without opening another file or remembering an issue number._

### Which comes first — the false-positive measurement, or the installer and Windows service that would make measuring it practical?

- **Status:** open · **Added:** unknown · **Reversible:** easily
- **Blocked:** Nothing is hard-blocked — but the download page's 'nobody has measured how often it cries wolf' stays true until this is answered, and it is the stated reason the product cannot be recommended to anyone.

**Background.** NiteWatch is currently a console application that a user starts by hand and that stops watching the moment the window closes. The shipped roadmap names two things as the next work: a measured false-positive rate ('nothing else on this list matters if the product cries wolf') and an installer that registers the agent as a Windows service. These are entangled. The false-positive number requires roughly a week of continuous quiet running on a real Windows desktop, which is exactly what a console app makes annoying — every reboot or accidental window close restarts the clock, and the one soak that has completed ran three days rather than seven. Kevin's own three-day soak in early August found five alerts, all false positives, which were traced to three specific causes and fixed on 2026-08-02 in commit d6179a6 (Windows Defender scanning credential stores from a ProgramData path the exclusion predicate did not cover, Chromium browsers importing each other's saved-password stores, and ordinary browser beaconing to ad and API endpoints). That was a genuinely good result — three fixable classes rather than random noise — but it measured the pre-fix build, so there is still no number describing how the shipped code behaves. Meanwhile the public download page tells every visitor the rate is unknown.

**Options**

| Option | Pros | Cons | Effort |
|---|---|---|---|
| Soak first on v0.1.6-pre as-is, accept the console-app friction | Costs no engineering time at all — it is Kevin leaving a window open. Produces the number that the product's own roadmap says gates everything else, and lets the download page's most damaging admission be replaced within about a week. Any false positives found feed straight into the next tuning pass. | Fragile: a reboot, a Windows update, or a closed console window silently truncates the run, and there is no way to tell a 7-day soak from a 7-day soak with three gaps in it. Consumes a week of calendar time during which no code ships. Only measures one machine — Kevin's — which is a sample of one for a consumer product. | weeks |
| Build the installer and service first, then soak against a properly unattended agent | Removes the top item on the shipped roadmap's Next list and the limitation the download page admits twice. Makes the soak reliable and repeatable rather than a one-shot manual vigil, and unblocks the third Next item (moving the SQLite ledger into %ProgramData% with an ACL), which is explicitly gated on the installer existing. Also makes the product something a stranger could plausibly install, which is what would generate the outside feedback the project currently gets none of. | Delays the number that matters most by the length of the installer work. Windows service installation, elevation, and service lifecycle are precisely the code paths that cannot be tested on the WSL2 dev box, so it needs a Windows VM that does not exist yet. Risks shipping a service that runs unattended before anyone knows how noisy it is. | weeks |
| Do neither yet — build the replayable false-positive regression suite from the traces already captured | Runs entirely on the dev box, needs no Windows VM and no calendar-week wait, and turns false-positive checking into a build gate that catches regressions forever rather than a measurement taken once. The soak-report tooling and replay source already exist. | Produces a regression guard, not a number — the download page's 'unmeasured' admission stays exactly as it is. A recorded trace from one machine also cannot surface false positives caused by software that machine does not run. Does not move the product any closer to something a stranger can install. | days |

**Recommendation.** Soak first on v0.1.6-pre. It costs no engineering time, it is the only item that can retire the single worst sentence on the public download page, and the three-class retune means this is the first run whose result would actually mean something; the installer is a multi-week Windows-VM project that will still be there afterwards.

### How does Kevin find out whether anyone has actually downloaded and run NiteWatch, given that the product forbids the telemetry that would tell him?

- **Status:** open · **Added:** unknown · **Reversible:** easily
- **Blocked:** Nothing technical, but every roadmap priority above is being ranked on an assumption about audience size that nobody has tested, and the portfolio's zero-open-issues gate reads as a pass here for the wrong reason.

**Background.** NiteWatch has been publicly downloadable from threattape.com since 2026-07-28 and is now on its sixth pre-release build. In that time zero GitHub issues have been filed — the repository has had exactly three issues ever, all opened by the development process itself and all closed by 2026-07-27. Three issue templates (bug, false-positive, config) were built specifically to receive outside reports and none has ever been used. The download page asks users to email threattape@gmail.com and calls that 'the most useful thing anyone can send me right now'. The product's hard constraint is that nothing about a user's machine is ever uploaded, so there is no phone-home, no update check, and no install counter — a constraint that was deliberately extended even to the shareware tip nag, which is unverified on purpose because checking would require a machine-identifying callback. The consequence is that 'zero open issues' — the portfolio's first ready-for-testing gate — carries no information here at all: it is equally consistent with a flawless product and with a product nobody has run. This matters for rotation planning, because NiteWatch keeps earning slots on the assumption there is an audience waiting on it.

**Options**

| Option | Pros | Cons | Effort |
|---|---|---|---|
| Read the web server access logs for download counts | Costs nothing, breaks no privacy promise — a request for a file on your own web server is not telemetry from the product, and the page already exists behind normal server logging. Answers the blunt question 'has anyone taken it' immediately, and distinguishes zero downloads from silent downloads, which are very different situations. | Counts downloads, not runs — someone who hits Smart App Control and cannot execute the file looks identical to a happy user. Says nothing about whether it produced false positives. Requires whatever log retention threattape.com's hosting actually keeps, which may be nothing. | hours |
| Add a prominent in-app prompt asking for a report, with a pre-filled GitHub issue link | Puts the feedback ask where the user actually is rather than on a page they have already left, and the pre-filled link removes the friction of composing an email. Consistent with the privacy stance because the user presses the button. The dashboard already has a Limits and roadmap panel to hang it off. | Requires a GitHub account, which most consumer users of a personal security tool will not have. Adds a nag to a product that already has a tip nag. Still yields nothing if nobody is running it, so it does not answer the underlying question. | hours |
| Accept that this is a build-in-public artifact with no measurable audience, and drop NiteWatch to a lower rotation cadence | Honest about the situation and frees rotation slots for the twenty-seven other projects. The product is feature-complete and publicly available; letting it sit costs nothing and it stays a credible portfolio piece. | Kills the momentum on the only consumer product in the portfolio, and does so on the basis of an absence of evidence that was never actually checked. The gap between 'feature complete' and 'installable by a stranger' is one installer wide, and abandoning it there wastes most of the value already built. | hours |

**Recommendation.** Check the server logs first — it is an hour of work and it converts a strategic question into an empirical one. Decide the rotation cadence after seeing whether the download count is zero, single digits, or something that justifies building the installer.

## Rotation History

_Newest first. Entries that covered several projects at once also appear in the other
projects' files and in `_archive/orchestrator-history/portfolio-sessions.md`._

> 🔵 **2026-07-29 (nitewatch — overnight soak found the OTHER half of the attribution bug; 0.1.1-pre and 0.1.2-pre published).** Kevin ran the agent overnight and woke to five critical alerts — a game launcher, a WebView host, an Electron app and a browser, all "reading your Discord session tokens" within a minute. **Root cause: recycled ETW file handles.** Kernel-File names a file once on Create and then identifies it by a FileKey/FileObject on every later read; that identifier is a kernel pointer, reused the moment the file closes. The name cache made it worse twice over — `put()` returned early when the key already existed, discarding the Create that announced the handle now referred to a different file, and nothing invalidated on Close/Cleanup. **The damage was selective, not random:** only paths worth alerting on are cached (the caller filters to user-profile files first), so the cache holds little but credential stores, and every collision resolved to something alarming. Discord's leveldb churns constantly, so its paths kept winning. **The tell was the "other" context lines, not the Discord ones:** Battle.net's agent reading Zoom's webview cache and Office rule files, brave.exe writing into Claude's leveldb — correct PIDs against wrong filenames. Fixed by rebinding on put, forgetting on close, and dropping a cached name when a key is seen naming an untracked path. **The cache moved to `internal/source/namecache.go` with no build tag** — it had lived inside the Windows-only ETW file, so it could not be exercised on any machine this project is developed on, which is most of why it survived; 5 tests now cover it and 3 fail against the old logic. **This is the second half of the attribution work — the 2026-07-28 PID-occupancy fix was real and necessary but was NOT the whole cause of "one event, several programs".** Also shipped: **sorting by Sent/Received compared byte counts as text** (flat list fell through to a String() default; the grouped view, which is the default, ignored the chosen column entirely while still drawing a sort arrow), and **`#build` never landed on the download section** — the browser resolves a hash once mid-layout, six unreserved screenshots pushed the target 2,600px, and web fonts reflowed it another 30px after `load`; fixed with intrinsic image dimensions, dropping a `width:auto` that defeated them, and a post-load correction that yields if the reader has already scrolled. **Releases:** `v0.1.1-pre` (1af6af5) then `v0.1.2-pre` (12677f9), each with its own tag and published SHA-256 rather than re-pointing a version — live SHA `9504f43f…`, verified byte-for-byte against the served file and the page. **Process lesson recorded in the README:** every build this session went to a scratchpad while Kevin was picking up `dist/`, so he soaked a 13:11 build (260bc24) that predated both attribution fixes; `dist/` is the documented pickup point and must be rebuilt, `-trimpath` added so a local build can match a published hash, and the file timestamp does not identify a build (Go stamps the COMMIT time). **Soak restarted on 0.1.2-pre 2026-07-29 — the first run on code where both attribution layers are fixed, so the first false-positive number that means anything.**

> 🔵 **2026-07-27→28 (nitewatch — Kevin-directed session, NOT a rotation; work is on an UNMERGED branch — see ⚠️ below).** Long interactive session driven by Kevin's hands-on use of the agent. **Shipped (25 commits on `fix/beacon-fp-and-db-lock`):** (1) **PID-occupancy attribution rewrite** — the graph kept one `procNode[pid]` pointer meaning "whoever holds that number NOW", so late-arriving ETW events were attributed to the current occupant; a live machine produced "brave.exe wrote files in AppData\\Roaming\\Claude" and the mirror-image claim in the same minute. Replaced with a `(PID, time) → occupant` tenure table keyed on Windows `ProcessStartKey` (`internal/graph/occupancy.go`), with three fallbacks where the key is absent (a start proves the previous tenure ended, so dropped exits self-repair; an event carrying its own image is evidence of the occupant; pre-history resolves to the oldest tenure). Growth is bounded by the existing window rotation, verified by a test that a live process keeps its original start time across a generation boundary. (2) **"Tip Uncle Grimmy" shareware nag** — honor system, deliberately unverified: checking would require a phone-home keyed to machine identity, which is the one thing the product's whole pitch forbids. Paced server-side (20h) so a reload cannot re-trigger it. (3) **Bigger disclosure arrows** + **the shipped docs were unreachable** — both Limits and Roadmap sat behind a button labelled "About" while the disclaimer told readers twice to look for "Limits in the header", a label that no longer existed. Now "Limits & roadmap" + a footer row. (4) **Dangling doc references** — the shipped Known Limitations pointed at `docs/plans/...md`, `docs/feed-licensing.md` and a Go test name; all three live in a **private** repo, so the pointer implied a withheld answer. Replaced with their substance, guarded by a test that fails the build on repo paths / source files / Go test names in either shipped document. **Real product bugs found by photographing the UI for marketing screenshots (a better bug-finder than reading the code):** **`bytes_sent`/`bytes_recv` were absent from the ledger INSERT column list** while still being passed as arguments — the driver accepted the surplus rather than rejecting the statement, so **every flow's first sighting recorded 0 bytes**; only repeat sightings accumulated, via the UPDATE branch. This hid from the agent too: an earlier screenshot showing "160 MB" against a one-event trace was four replays into an undeleted DB. Volume is the signal that separates a heartbeat from an upload, and a one-shot exfiltration was exactly the case reading zero. Also `who owns [object Object]` in the RDAP disclosure sentence, and literal asterisks in the shipped docs (renderer handled `**bold**` but not `*emphasis*`). Error from `RecordConnectionDedup` is no longer discarded. **Marketing (threattape, deployed to prod, verified live):** six real screenshots on `/products/nitewatch/`, a "Can I run it?" section, and a **correction — both the subsite and walkthrough claimed the RDAP lookup was "the one outbound query"; there are two now (VirusTotal). Dossier + work card "your data never goes up" → "nothing goes up on its own".** **Code signing: KEVIN SAYS NO (not paying).** Researched first and the finding changes the calculus — Microsoft withdrew instant SmartScreen reputation for EV certs and their current guidance says paying a premium for EV to avoid those warnings "is no longer justified"; every purchasable cert still warns on first download, only the Store avoids it. So signing buys a publisher name in the dialog + reputation carrying between releases, not warning removal. Moved out of the roadmap's "Next" into a new **"Decided against, for now"** section (reversible cost decision, unlike the kernel driver in "Never"). **Consequence now stated plainly in the shipped limitations and on the site: on Windows 11 with Smart App Control enabled the unsigned binary will not run at all** — a harder gate than SmartScreen. **RESOLVED same session: PR [#6](https://github.com/GrimthornRedbeard/nitewatch/pull/6)** opened for all 26 commits (branch `fix/beacon-fp-and-db-lock` → `main`, which had been stale at `e5bb94a`). **Kevin cleared Sparks** ("we're good on Sparks"), so the disclaimer gate is closed and **Kevin authorised publishing the download** ("wire it up"). **Added for the release:** `internal/buildinfo` reads Go's own VCS stamping (revision / build time / dirty flag) rather than an ldflags value that can drift, surfaced in the About panel as a selectable line labelled "quote this in any bug report" — a public download generates bug reports and "which build?" previously had no answer a user could find. Version `0.1.0-dev` → `0.1.0-pre`. **Provenance note:** the exe Kevin is soaking is stamped `vcs.revision=b535c93, modified=true` — same source as `23e92ed`, built minutes before that commit landed, so it is source-identical but NOT byte-identical to anything on main. Builds are otherwise deterministic (two rebuilds from a clean tree are byte-identical), so the published artifact will reproduce from its tag. **PUBLISHED 2026-07-28:** PR #6 merged (squash, `493d778`), tagged **`v0.1.0-pre`**, release built from that tag with a clean tree and published at https://threattape.com/products/nitewatch/#build — SHA-256 `055a032f…18a31bf`, verified byte-for-byte across the local build, the file the server hands out, and the hash printed on the page. Copilot: 1 cycle, 2 inline findings, both real — settings kept untrimmed values in memory (`vt.New` received a whitespace-padded key straight after save, so a pasted VirusTotal key failed all session then silently worked after a restart), and a ledger pool comment describing an intent the code never implemented (**pushed back on the suggested fix** — setting the pool to 1 would queue dashboard reads behind every write, re-creating the contention the WAL switch removed; corrected the comment instead). **Gate status: still NOT the five-gate "ready for testing" signal.** Gates 1+2 pass but partly *because* nothing was filed; Gate 3 passes (23 packages green under `-race`); Gates 4–5 not formally run this session. **The false-positive soak Kevin is running is the one remaining engineering unknown** — the roadmap's own "nothing else matters if it cries wolf" gate — and the download page says so in as many words rather than hiding it.
